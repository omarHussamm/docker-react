sudo: required 
services: 
  - docker

before_install: 
  - docker build -t omarhussammostafa/docker-react -f Dockerfile.dev .

script: 
  - docker run -e CI=true omarhussammostafa/docker-react npm run test